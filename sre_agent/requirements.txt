# SRE Agent — standalone dependencies

# Workflow engine
langgraph>=0.3.27
langchain-core>=0.3.0
langchain-openai>=0.2.0     # optional at runtime; heuristics used when unconfigured

# Event-driven trigger
azure-servicebus>=7.11.0
azure-functions>=1.18.0     # only needed for the Functions trigger surface
azure-identity>=1.15.0      # live observability queries
azure-monitor-query>=1.2.0  # live observability queries

# Models & config
pydantic>=2.5.3
pydantic-settings>=2.1.0
pyyaml>=6.0.1

# HTTP (ticketing / deployment correlation)
httpx>=0.26.0

# Production (required when SRE_AGENT_ENVIRONMENT=production)
# langgraph-checkpoint-postgres>=2.0.0  # durable checkpointer (brings psycopg,
#                                       # also used by the alert ledger and
#                                       # pending-approval stores)
# azure-monitor-opentelemetry>=1.6.0    # export step logs/spans to App Insights
#                                       # (enabled by APPLICATIONINSIGHTS_CONNECTION_STRING)

# Semantic knowledge retrieval (pgvector backend). Embeddings use the
# langchain-openai dep already listed above; pgvector needs psycopg (from
# langgraph-checkpoint-postgres) plus the pgvector adapter:
# pgvector>=0.3.0   # required when SRE_AGENT_KNOWLEDGE_VECTOR_BACKEND=pgvector

# Optional extras
# langchain-mcp-adapters>=0.1.0   # enable MCP connectors (sre_agent/mcp)

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
